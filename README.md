# laughing-giggle

Minimal scaffold created by assistant.

Quick start

```bash
# install dependencies
npm install

# start server
npm start

# test
curl http://localhost:3000/
```

The server responds with JSON { "message": "Hello from laughing-giggle!" }.